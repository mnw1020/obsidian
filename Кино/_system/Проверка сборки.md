# Проверка сборки

Дата: 24.09.2026

## Итог

- Технических ошибок статической проверки: **0**.
- Основных карточек: **1553**.
- Файлов ролей: **1553**.
- Системных JavaScript-файлов: **18**, все проходят `node --check`.
- DataviewJS-блоков: **3285**; уникальных вариантов: **11**, все проходят синтаксическую проверку.
- JSON-файлов: **4**, все корректно разбираются.
- `build_movielens_model.py` проходит `py_compile`.
- `_Кино.base` корректно разбирается как YAML.
- Все карточки содержат один раскрывающийся блок ролей V2 и одну кнопку `🔎 Найти похожие` V2.
- У кнопки есть пустая строка до и после, а также визуальные отступы.
- Скрипты просмотров, сезонов и пересборки карточки сохраняют постоянные блоки ролей и рекомендаций.
- KP API подключен в добавление фильма, обновление ролей и рекомендации; резервные источники сохранены.

## Неисправленные предупреждения о данных

Есть одна существующая неполная карточка `Кино/Без названия.md`:

- одновременно стоят теги `movies` и `serial`;
- нет `Кинопоиск ID`;
- нет осмысленного названия/метаданных.

Она не удалялась и не переопределялась автоматически, чтобы не потерять пользовательские данные. Companion-файл ролей для неё создан, поэтому служебная структура не разорвана.

## Повторная проверка

Архив был упакован, затем повторно распакован во временную папку и снова прошёл тот же набор проверок: **0 технических ошибок**. Предупреждения остались только по существующей неполной карточке `Без названия.md`.
