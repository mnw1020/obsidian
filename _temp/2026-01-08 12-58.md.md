---
autor: Name
title: Name
date: 2026-01-08
rating: 0-10
modify: 2026-01-08 12:43
---
<%*
// Создаём путь для нового файла
const folder = "_temp";
const fileName = tp.date.now("YYYY-MM-DD HH-mm"); // можно изменить на любое имя
const path = `${folder}/${fileName}.md`;

// Перемещаем текущий файл в папку _temp/new с нужным именем
await tp.file.move(path);
%>

текст


```button
name Make an Hourly Note
type note(<% tp.date.now("HH:MM") %>)
action "Новая заметка"
templater true
```



```button
name Manage Field
type chain
actions [
  {"type": "append text", "action": "exercise::"},
  {"type": "command", "action": "Templater: Create new note from template"}
]
```
exercise::
exercise::
exercise::


```button
name Manage Field
type chain
actions [
  {"type": "append text", "action": "exercise::"},
  {"type": "command", "action": "

текст


```button
exercise::
exercise::
exercise::
exercise::
exercise::
name Make an Hourly Note
type note(12:01)
action "Новая заметка"
templater true
```



```button
name Manage Field
type chain
actions [
  {"type": "append text", "action": "exercise::"},
  {"type": "command", "action": "Templater: Create new note from template"}
]
```
exercise::
exercise::
exercise::


```button
name Manage Field
type chain
actions [
  {"type": "append text", "action": "exercise::"},
  {"type": "command", "action": "Templater: Insert template", "template": "_templates/Create New Note"}
]
```
exercise::
", "template": "_templates/Create New Note"}
]
```
exercise::
